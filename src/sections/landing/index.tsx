import { Container } from '../../components/container'
import { Header } from '../../components/header'

export const Landing = () => {
    return (
        <Container.Section>
            <Header />
        </Container.Section>
    )
}
